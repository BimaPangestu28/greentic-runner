id: demo.flow
type: messaging
start: qa
nodes:
  qa:
    component.exec:
      component: qa.process
      operation: process
      input:
        text: "hello"
    routing:
      - to: emit
  emit:
    emit.response:
      text: "Echo: {{node.qa.text}}"
    routing:
      - out: true
